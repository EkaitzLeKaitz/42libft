/*
lst_delone => Toma como parametro un nodo lst y libera la memoria del contenido
utilizando la funcion del, además de liberar el nodo. La memoria de next no
debe liberarse.
*/
#include <stdio.h>
#include "libft.h"
/*
static void	*del(*lst)
{
	free(lst);
}

void	ft_lstdelone(t_list *lst, void (*del)(void *))
{
	if (!lst || !del)
		return ;
}

int	main(void)
{
}*/
